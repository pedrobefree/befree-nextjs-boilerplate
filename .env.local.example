# Supabase Configuration
# Obtain these from your Supabase Dashboard -> Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service Role Key (SERVER SIDE ONLY)
# NEVER expose this to the client. Used for admin operations and bypass RLS.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Encryption Key for sensitive data
# Generate a 32-byte key: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=your-base64-32-byte-key

# App Settings
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
